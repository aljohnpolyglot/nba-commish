import { SocialTemplate } from '../types';

// Game-context meme templates are disabled.
// NBA memes are now fired from nbaMemesFetcher.ts in socialHandler.ts —
// fetched real tweets that post nonsensically into the feed regardless of game results.
export const NBA_MEMES_TEMPLATES: SocialTemplate[] = [];
